/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MnemoHighlightDirective } from './mnemoHighlight.directive';

describe('Directive: MnemoHighlight', () => {
  it('should create an instance', () => {
    const directive = new MnemoHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
